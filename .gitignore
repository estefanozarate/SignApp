node_modules/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/

# Firmas — nunca subir un keystore de release
*.keystore
*.jks
!debug.keystore

# La clave privada del dominio. Si esto se filtra, cualquiera puede hacerse
# pasar por el dominio ante las apps que ya emparejaron.
server/datos/
*.pem

# macOS
.DS_Store

# Entorno
.env
.env.local

# Metro / logs
.metro-health-check*
*.log
npm-debug.log*
yarn-error.log*
